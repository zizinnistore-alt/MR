<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تقرير أداء الطالب: <%= student.student_name %></title>

    <!-- PWA & Mobile App Settings (Placeholders) -->
    <link rel="manifest" id="pwa-manifest" href=""> <!-- سيتم ملؤه بالكامل بواسطة JS -->
    <meta name="theme-color" content="#4f46e5">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="تقريري">
    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">

    <!-- External Libraries -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;900&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/html5-qrcode" type="text/javascript"></script>

    <style>
        body { font-family: 'Cairo', sans-serif; }
        :root { direction: rtl; }
        #qr-reader { width: 100%; max-width: 500px; margin: 1rem auto; border: 2px solid #ccc; border-radius: 8px; }
        #qr-reader-results { margin-top: 1rem; font-weight: bold; text-align: center; }
    </style>
</head>

<body class="bg-gray-100 text-gray-800">

    <!-- زر التثبيت - سيظهر دائماً -->
    <div id="install-pwa-container" class="fixed bottom-6 left-6 z-50">
        <button id="install-pwa-button" class="bg-teal-500 text-white rounded-lg px-4 py-3 flex items-center justify-center shadow-lg">
            <i class="fas fa-download mr-2"></i>
            <span class="font-bold">تثبيت التطبيق</span>
        </button>
    </div>

    <div class="container mx-auto p-4 md:p-8">
        <!-- Student Header -->
        <div class="text-center mb-8 p-6 bg-white rounded-xl shadow-md">
            <h1 class="text-3xl md:text-4xl font-extrabold text-indigo-700">مرحباً، <%= student.student_name %></h1>
            <p class="text-gray-500 mt-2">هذا هو تقرير أدائك الدراسي المفصل</p>
        </div>

        <!-- QR Code Scanner Section -->
        <div class="my-8 text-center">
            <button id="start-scan-btn" class="bg-teal-500 hover:bg-teal-600 text-white font-bold py-3 px-6 rounded-lg">
                <i class="fas fa-qrcode mr-2"></i> تسجيل الحضور بالـ QR
            </button>
            <div id="qr-reader" style="display: none;"></div>
            <div id="qr-reader-results"></div>
        </div>

        <!-- Motivational Message -->
        <% 
            let message = '';
            let messageClass = '';
            if (stats.session.rankPercentage <= 10) {
                message = `ممتاز! أنت من أفضل 10% من الطلاب. استمر في هذا الأداء الرائع! 🚀`;
                messageClass = 'bg-green-100 border-green-500 text-green-800';
            } else if (stats.session.rankPercentage >= 80) {
                message = `تحتاج إلى بذل المزيد من الجهد. نحن نثق بقدراتك! 💪`;
                messageClass = 'bg-yellow-100 border-yellow-500 text-yellow-800';
            }
        %>
        <% if (message) { %>
            <div class="p-4 mb-8 border-r-4 rounded-lg shadow-sm <%= messageClass %>">
                <p class="font-bold"><%= message %></p>
            </div>
        <% } %>

        <!-- Stats Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="bg-white p-6 rounded-xl shadow-md text-center">
                <h3 class="text-lg font-bold text-gray-500">متوسط درجات الحصص</h3>
                <p class="text-4xl font-extrabold text-indigo-600 mt-2"><%= stats.session.average.toFixed(1) %></p>
                <p class="text-sm text-gray-400 mt-1">متوسط الصف: <%= stats.session.classAverage.toFixed(1) %></p>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-md text-center">
                <h3 class="text-lg font-bold text-gray-500">ترتيبك في الحصص</h3>
                <p class="text-4xl font-extrabold text-indigo-600 mt-2"><%= stats.session.rank %></p>
                 <p class="text-sm text-gray-400 mt-1">ضمن أفضل <%= stats.session.rankPercentage.toFixed(0) %>%</p>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-md text-center">
                <h3 class="text-lg font-bold text-gray-500">نسبة الحضور</h3>
                <% 
                    const sessionsForAttendance = (data && data.sessions) ? data.sessions : [];
                    const totalSessions = sessionsForAttendance.length;
                    const attendedSessions = sessionsForAttendance.filter(s => s && s.attendance === 'حاضر').length;
                    const attendancePercentage = totalSessions > 0 ? (attendedSessions / totalSessions) * 100 : 0;
                %>
                <p class="text-4xl font-extrabold text-indigo-600 mt-2"><%= attendancePercentage.toFixed(0) %>%</p>
                <p class="text-sm text-gray-400 mt-1"><%= attendedSessions %> من <%= totalSessions %> حصص</p>
            </div>
        </div>

        <!-- Detailed Grades Tables -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            <div class="bg-white p-6 rounded-xl shadow-md">
                <h2 class="text-2xl font-bold mb-4">الامتحانات الكبرى</h2>
                <div class="space-y-4">
                <% Object.keys(stats.exams).forEach(examName => { %>
                    <div class="border border-gray-200 p-4 rounded-lg">
                        <h4 class="font-bold text-lg text-indigo-700"><%= examName %></h4>
                        <div class="flex justify-between items-center mt-2 text-sm">
                            <span class="font-semibold">درجتك: <span class="text-xl font-extrabold <%= stats.exams[examName].grade > (stats.exams[examName].classAverage) ? 'text-green-500' : 'text-red-500' %>"><%= stats.exams[examName].grade %></span></span>
                            <span>متوسط الصف: <%= stats.exams[examName].classAverage %></span>
                            <span>ترتيبك: <%= stats.exams[examName].rank %> / <%= stats.exams[examName].totalStudents %></span>
                        </div>
                    </div>
                <% }); %>
                </div>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-md">
                <h2 class="text-2xl font-bold mb-4">درجات الحصص</h2>
                <div class="max-h-96 overflow-y-auto">
                    <table class="w-full text-center">
                        <thead class="bg-gray-100">
                            <tr><th class="p-3 font-bold">الحصة</th><th class="p-3 font-bold">الدرجة</th><th class="p-3 font-bold">الحضور</th></tr>
                        </thead>
                        <tbody>
                            <% if (data && data.sessions && data.sessions.length > 0) { %>
                                <% data.sessions.forEach((session, index) => { %>
                                    <% if (session) { %>
                                        <tr class="border-b"><td class="p-3">حصة <%= index + 1 %></td><td class="p-3 font-bold <%= session.grade > 5 ? 'text-green-600' : 'text-red-600' %>"><%= session.grade ?? '-' %></td><td class="p-3"><span class="px-2 py-1 text-xs rounded-full <%= session.attendance === 'حاضر' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>"><%= session.attendance ?? 'لم يسجل' %></span></td></tr>
                                    <% } %>
                                <% }); %>
                            <% } else { %>
                                <tr><td colspan="3" class="p-4 text-center text-gray-500">لا توجد بيانات حصص لعرضها.</td></tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- =================================================================== -->
    <!-- ============== START: ALL PAGE SCRIPTS (SELF-CONTAINED) =========== -->
    <!-- =================================================================== -->

    <!-- Script 1: QR Scanner Logic (لا يتغير) -->
    <script>
        // ... كود QR Scanner كما هو ...
    </script>

    <!-- Script 2: The Final, All-in-One, Self-Contained PWA Logic -->
    <script>
        // هذا السكريبت يقوم بكل شيء: ينشئ الـ Manifest، يسجل الـ Service Worker، ويتحكم في الزر
        (function() {
            // متغير عالمي ضمن هذا النطاق لحفظ فرصة التثبيت
            let deferredPrompt;

            // --- الخطوة 1: تجهيز كل شيء عند تحميل الصفحة ---
            window.addEventListener('load', () => {
                
                // الجزء (أ): إنشاء الـ Manifest الديناميكي من الصفر
                const studentCode = "<%= student.unique_code %>".trim();
                const studentName = "<%= student.student_name %>";
                
                const dynamicManifest = {
                    
    "name": "تقرير الطالب الدراسي",
    "short_name": "تقريري",
    "description": "تطبيق لمتابعة الأداء الدراسي والدرجات للطلاب.",
    "start_url": "/",
    "display": "standalone",
    "background_color": "#ffffff",
    "theme_color": "#4f46e5",
    "icons": [
      {
        "src": "/icons/icon-192x192.png",
        "type": "image/png",
        "sizes": "192x192"
      },
      {
        "src": "/icons/icon-512x512.png",
        "type": "image/png",
        "sizes": "512x512"
      }
    ]
  
                };
                const manifestString = JSON.stringify(dynamicManifest);
                const blob = new Blob([manifestString], { type: 'application/json' });
                const manifestURL = URL.createObjectURL(blob);
                document.querySelector('#pwa-manifest').setAttribute('href', manifestURL);
                console.log('PWA: الـ Manifest الديناميكي تم إنشاؤه وتعيينه بنجاح.');

                // الجزء (ب): تسجيل الـ Service Worker
                if ('serviceWorker' in navigator) {
                    navigator.serviceWorker.register('/service-worker.js')
                        .then(reg => console.log('PWA: الـ Service Worker تم تسجيله بنجاح.', reg))
                        .catch(err => console.error('PWA: فشل تسجيل الـ Service Worker:', err));
                }
            });

            // --- الخطوة 2: الاستماع لفرصة التثبيت وتخزينها ---
            window.addEventListener('beforeinstallprompt', (e) => {
                e.preventDefault(); // منع الإشعار التلقائي
                deferredPrompt = e; // تخزين الفرصة
                console.log('PWA: فرصة التثبيت متاحة الآن! (`beforeinstallprompt` event fired).');
            });

            // --- الخطوة 3: التحكم في الزر الظاهر دائماً ---
            const installButton = document.getElementById('install-pwa-button');
            installButton.addEventListener('click', async () => {
                if (deferredPrompt) {
                    // الحالة الناجحة: الفرصة موجودة، اطلب التثبيت
                    console.log('PWA: سيتم الآن عرض نافذة التثبيت.');
                    deferredPrompt.prompt();
                    const { outcome } = await deferredPrompt.userChoice;
                    console.log(`PWA: رد المستخدم كان: ${outcome}`);
                    if (outcome === 'accepted') {
                        document.getElementById('install-pwa-container').style.display = 'none'; // أخفِ الزر بعد القبول
                    }
                    deferredPrompt = null; // لا يمكن استخدام الفرصة مرة أخرى
                } else {
                    // الحالة الفاشلة: الفرصة غير متاحة
                    console.error('PWA: لا يمكن بدء التثبيت. `deferredPrompt` غير متاح.');
                    alert('التطبيق غير جاهز للتثبيت.\n\nتأكد من:\n1. أنك تستخدم رابط https أو localhost.\n2. أن أيقونات PWA موجودة في مجلد public/icons.\n3. أن ملف service-worker.js موجود وصحيح.\n\nراجع الـ Console أو استخدم Lighthouse لمزيد من التفاصيل.');
                }
            });

            // --- الخطوة 4: (اختياري) الاستماع لحدث اكتمال التثبيت ---
            window.addEventListener('appinstalled', () => {
                console.log('PWA: تم تثبيت التطبيق بنجاح!');
                document.getElementById('install-pwa-container').style.display = 'none';
            });

        })();
    </script>

</body>
</html>